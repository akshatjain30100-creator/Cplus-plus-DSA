#include <iostream>
#include <math.h>
using namespaces as std 
int main()
{
       int num,rom,cube,sum=0,digit;
       cout>>"enter a no"
       cin<<num;
       rom=num;
       while(num!=0)
       {
         digit = num%10;
         cube = digit * digit * digit; 
          sum=
       }
}